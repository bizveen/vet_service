
import 'package:flutter/material.dart';

import '../../models/complain/Treatment.dart';

class EditTreatmentScreen extends StatelessWidget {
  Treatment treatment;
   EditTreatmentScreen({Key? key, required this.treatment}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
